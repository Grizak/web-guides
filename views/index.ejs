<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Web Development Guides</title>
    <link rel="stylesheet" href="/css/home.css">
</head>
<body>
    <header>
        <h1>Welcome to Web Development Guides</h1>
        <p>Learn web development with our curated guides.</p>
        <% if (admin) { %>
            <p>Hi <strong><%= admin.username %></strong></p>
            <a href="/admin">Dashboard</a>
            <a href="/logout">Logout</a>
        <% } else { %>
            <a href="/login">Login for admins</a>
        <% } %>
    </header>

    <main>
        <h2>Available Guides</h2>
        <% if (guides.length > 0) { %>
            <ul>
                <% guides.forEach(guide => { %>
                    <li>
                        <a href="/guides/<%= guide.name %>"><%= guide.title %></a>
                        <small>(Category: <%= guide.category.name %>)</small>
                    </li>
                <% }) %>
            </ul>
        <% } else { %>
            <p>No guides available yet. Check back soon!</p>
        <% } %>
    </main>
</body>
</html>
